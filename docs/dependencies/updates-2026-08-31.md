# Dependency updates — 2026-08-31

## Summary

Updated all direct JavaScript packages reported by `bun outdated`, retained and
advanced the xterm prerelease channel, and refreshed both Rust lockfiles to
their newest compatible resolutions. No dependency-required source or
configuration migration was needed.

No repository-managed package patches are active in this checkout. The
reference to a historical patch in `CLAUDE.md` does not correspond to a current
patch file or Bun patch configuration.

## Applied JavaScript updates

- Root: `turbo` 2.10.10 -> 2.10.12.
- Desktop: `@pierre/diffs` 1.3.5 -> 1.3.6, `@sentry/react` 10.70.0 ->
  10.72.0, `@tanstack/react-router` 1.170.29 -> 1.170.32,
  `@tanstack/react-virtual` 3.14.9 -> 3.14.10, `dompurify` 3.4.13 ->
  3.4.14, `lucide-react` 1.31.0 -> 1.38.0, `shadcn` 4.18.0 -> 4.19.0,
  and `streamdown` 2.5.0 -> 2.6.0.
- Desktop development dependencies: `@types/react-dom` 19.2.4 -> 19.2.5,
  `@vitejs/plugin-react` 6.0.5 -> 6.1.1, and `vite` 8.2.1 -> 8.2.2.
- Prerelease checks: `@pierre/trees` remains at its newest beta,
  1.0.0-beta.6. The xterm packages' `latest` tags point to lower stable
  releases, so those results were rejected. Their current beta tags were
  applied instead: `@xterm/addon-fit` 0.12.0-beta.299 ->
  0.12.0-beta.301, `@xterm/addon-search` 0.17.0-beta.299 ->
  0.17.0-beta.301, `@xterm/addon-webgl` 0.20.0-beta.298 ->
  0.20.0-beta.300, and `@xterm/xterm` 6.1.0-beta.302 ->
  6.1.0-beta.304.

## Applied Rust updates

- `backend/tauri/Cargo.lock`: refreshed 39 compatible resolutions, including
  `blocking` 1.6.2 -> 1.7.0, `h2` 0.4.15 -> 0.4.19, `hyper` 1.11.0 ->
  1.11.1, `lru` 0.18.2 -> 0.18.3, the Sentry 0.49.1 -> 0.49.2 family,
  and `uuid` 1.24.1 -> 1.26.0.
- `backend/mcp/Cargo.lock`: refreshed 25 compatible resolutions, including
  `h2` 0.4.15 -> 0.4.19, `hyper` 1.11.0 -> 1.11.1, the Sentry 0.49.1 ->
  0.49.2 family, and `uuid` 1.24.1 -> 1.26.0.
- `cargo-outdated`, `cargo-upgrade`, and `cargo-audit` were not installed, so
  no global tools were added. The direct requirements were inspected and
  already use the current compatible major/minor ranges; no manifest change
  was needed.

## Source migrations

None required. The JavaScript changes are patch/minor updates except for the
maintained prerelease family, and the refreshed Rust resolutions compile and
test without source or configuration changes.

## Security advisories before and after

`bun audit` reported 64 advisories both before and after: 15 high, 42
moderate, and 7 low. They are transitive dependency paths through `shadcn`,
`streamdown`/Mermaid, Vite, and Sentry tooling. No available direct update in
this run removes them.

## Deferred updates and exact blockers

No direct dependency update was deferred. The 64 remaining JavaScript
advisories are deferred pending compatible upstream dependency resolutions;
forcing transitive overrides without upstream compatibility evidence was out of
scope. Rust advisory scans were not run because `cargo-audit` is not installed.

## Validation

- Initial `bun outdated` at the repository root and in `apps/desktop`:
  passed and reported the direct JavaScript candidates applied above.
- Initial `bun audit`: completed with exit 1 after reporting 64 advisories
  (15 high, 42 moderate, 7 low).
- Initial `cargo update --dry-run` in `backend/tauri`: passed and reported 39
  compatible lockfile updates.
- Initial `cargo update --dry-run` in `backend/mcp`: passed and reported 25
  compatible lockfile updates.
- `bun update --latest` at the root and in `apps/desktop`: passed. Explicit
  prerelease dist-tag checks restored the xterm beta channel after the command
  proposed lower stable versions.
- `cargo update` in both Rust workspaces: passed.
- `bun install`: passed.
- `bun run typecheck`: passed.
- `(cd apps/desktop && bun run build)`: passed. Vite emitted existing
  future-native-config-loader and chunk-size warnings.
- `(cd backend/tauri && cargo check --workspace)`: passed after the checked-in
  sidecar build scripts supplied the ignored `externalBin` files required by a
  fresh worktree.
- `(cd backend/tauri && cargo test --workspace)`: the default command passed
  198 host tests and skipped 3 live-service tests, then the nested shared
  crate failed because its checked-in relative macOS runner path does not
  resolve from that crate. Rerunning with the same `scripts/dev-sign.sh`
  runner provided by absolute path passed all suites: 198 host tests, 14 shared
  tests, and 6 daemon tests; the same 3 host tests remained skipped.
- `(cd backend/mcp && cargo check && cargo test)`: passed; 5 tests passed.
- Final `bun outdated` at the root and in `apps/desktop`: passed with no
  remaining direct updates. The prerelease channels were verified separately.
- Final `cargo update --dry-run` in both Rust workspaces: passed with zero
  compatible packages to update.
- Final `bun audit`: completed with exit 1 and the unchanged 64 transitive
  advisories.
- `git diff --check`: passed after generated validation output was removed.
