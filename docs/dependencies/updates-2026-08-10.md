# Dependency updates -- 2026-08-10

## Summary

Updated every current direct JavaScript release, preserved and advanced the
repository's xterm beta channel, refreshed both Rust lockfiles, and aligned the
Tauri host, PTY daemon, and Tauri Sentry plugin on Sentry 0.49. All prescribed
typechecks, production builds, Rust checks, and Rust tests pass.

No repository-managed package patches are active in this checkout.
`cargo-outdated`, `cargo-upgrade`, and `cargo-audit` are not installed, so no
global Cargo tools were added.

## Applied JavaScript updates

- `turbo`: 2.10.8 -> 2.10.9
- `@base-ui/react`: 1.6.0 -> 1.7.0
- `@codemirror/lang-html`: 6.4.11 -> 6.4.12
- `@codemirror/lang-markdown`: 6.5.1 -> 6.5.2
- `@codemirror/view`: 6.43.7 -> 6.43.8, including the root override
- `@pierre/diffs`: 1.3.1 -> 1.3.5
- `@tanstack/react-router`: 1.170.18 -> 1.170.25
- `@xterm/addon-fit`: 0.12.0-beta.292 -> 0.12.0-beta.298
- `@xterm/addon-search`: 0.17.0-beta.292 -> 0.17.0-beta.298
- `@xterm/addon-webgl`: 0.20.0-beta.291 -> 0.20.0-beta.297
- `@xterm/xterm`: 6.1.0-beta.292 -> 6.1.0-beta.300
- `dompurify`: 3.4.12 -> 3.4.13
- `lucide-react`: 1.28.0 -> 1.31.0
- `shadcn`: 4.16.1 -> 4.16.2
- `sonner`: 2.0.7 -> 2.0.8
- `vite`: 8.2.0 -> 8.2.1

`bun update --latest` initially followed the stable xterm `latest` tags and
proposed four semantic downgrades. Explicit registry dist-tag checks showed
newer beta releases, so the four packages were advanced together on the beta
channel. `@pierre/trees` 1.0.0-beta.6 remains the newest published beta.
Every accepted direct JavaScript change has higher semantic-version precedence
than its previous version.

## Applied Rust updates

### Tauri workspace

- Direct host and daemon `sentry` requirements: 0.42 -> 0.49.
- Direct `tauri-plugin-sentry` requirement: 0.5 -> 0.6. The published 0.6
  metadata pairs it with Sentry 0.49, removing the previous compatibility
  blocker and the duplicate Sentry 0.42 stack.
- The lockfile refreshed 69 package operations, including Sentry 0.49.1,
  `tauri-plugin-sentry` 0.6.0, `sentry-rust-minidump` 0.17.0, `rusqlite`
  0.40.2, `base64` 0.23.1, `ignore` 0.4.33, `lru` 0.18.2, and current
  compatible networking, WebAssembly, platform, and proc-macro crates.

### MCP workspace

- The lockfile refreshed 31 compatible packages, including Sentry 0.49.1,
  `rusqlite` 0.40.2, `base64` 0.23.1, `aws-lc-rs` 1.18.0, and the current
  compatible networking and WebAssembly crates.

Final verbose Cargo dry-runs report zero compatible packages left to update in
either workspace. The few older Tauri lockfile entries are transitive version
constraints.

## Source migrations

Sentry 0.49 makes `ClientOptions` non-exhaustive and replaces the direct
`traces_sample_rate` field with a builder method. The Tauri host and PTY daemon
now use the same `ClientOptions` builder pattern already used by MCP, preserving
their release, environment, stacktrace, PII, logging, and trace-sampling
settings.

No JavaScript source or configuration migration was required.

## Security advisories before and after

- Before: 65 JavaScript advisories -- 15 high, 43 moderate, and 7 low.
- After: 64 JavaScript advisories -- 15 high, 42 moderate, and 7 low.

The remaining advisories are transitive dependencies reached through the
latest `shadcn`, Sentry Vite plugin, Streamdown, Mermaid, and Vite graphs. No
available direct update removes them; unrelated transitive overrides were not
forced without upstream compatibility evidence. Rust advisory scans were not
run because `cargo-audit` is not installed.

## Deferred updates and exact blockers

- No direct dependency update is deferred.
- The 64 remaining JavaScript advisories are deferred pending compatible fixed
  upstream dependency graphs.

## Validation

- Initial `bun outdated` at the root and in `apps/desktop`: passed and reported
  the JavaScript candidates applied above.
- Initial `bun audit`: completed with exit 1 after finding 65 advisories (15
  high, 43 moderate, 7 low).
- Initial `cargo update --dry-run --verbose` in `backend/tauri`: passed and
  reported 49 compatible lockfile updates before the Sentry requirement
  migration.
- Initial `cargo update --dry-run --verbose` in `backend/mcp`: passed and
  reported 31 compatible lockfile updates.
- `bun update --latest` at the root and in `apps/desktop`: passed. The xterm
  stable-tag downgrades were replaced with the current beta dist-tags.
- `cargo update` in both Rust workspaces: passed.
- `bun install`: passed with 839 packages checked after the final CodeMirror
  override alignment.
- `bun run typecheck`: passed.
- `(cd apps/desktop && bun run build)`: passed with Vite 8.2.1. Vite emitted
  its existing future native-config-loader and large-chunk warnings.
- `(cd backend/tauri && cargo check --workspace)`: passed. Because the volume
  initially had only 115 MiB free, the successful validation disabled debug
  symbols and incremental output after cleaning only this worktree's generated
  Cargo artifacts.
- `(cd backend/tauri && cargo test --workspace)`: 153 tests passed. The
  repository's relative macOS runner path fails for nested workspace packages,
  so the successful rerun supplied the same checked-in `scripts/dev-sign.sh`
  runner by absolute path. Debug symbols and incremental output were disabled
  to fit the constrained volume.
- `(cd backend/mcp && cargo check)`: passed.
- `(cd backend/mcp && cargo test)`: passed (one integration test; the binary
  has no unit tests). The integration test had one transient OS `WouldBlock`
  resource failure, then passed both an isolated retry and the full rerun.
- Final `bun outdated` at both JavaScript workspace locations: passed with no
  updates reported.
- Final `bun audit`: completed with exit 1 after finding 64 transitive
  advisories (15 high, 42 moderate, 7 low).
- Final `cargo update --dry-run --verbose` in both Rust workspaces: passed with
  zero compatible packages to update.
- `cargo audit` was not run because it is not installed.
