# Dependency updates — 2026-07-24

## Summary

Updated the JavaScript manifests and lockfile to current releases, refreshed both Rust lockfiles to their latest compatible versions, and validated the frontend and both Rust workspaces. Packages already on the xterm beta channel were kept on that channel and advanced to the registry's current beta tags instead of being downgraded to older stable releases.

No active package patches were present in the checkout. `cargo-outdated`, `cargo-upgrade`, and `cargo-audit` were not installed, so no global Cargo tools were added.

## Applied JavaScript updates

- Tooling: Turbo 2.10.1 → 2.10.6, TypeScript 6.0.3 → 7.0.2, Vite 8.1.1 → 8.1.5, and `@vitejs/plugin-react` 6.0.3 → 6.0.4.
- React: React and React DOM 19.2.7 → 19.2.8. The React type packages were already current and remain aligned on 19.2.x.
- Tailwind: Tailwind CSS and `@tailwindcss/vite` 4.3.2 → 4.3.3.
- Xterm beta channel: `@xterm/xterm` 6.1.0-beta.288 → 6.1.0-beta.291, fit 0.12.0-beta.288 → beta.291, search 0.17.0-beta.288 → beta.291, and WebGL 0.20.0-beta.287 → beta.290. These are the registry's current `beta` dist-tags.
- CodeMirror: Markdown 6.5.0 → 6.5.1, state 6.7.0 → 6.7.1, and view 6.43.4 → 6.43.6.
- Observability and UI: `@sentry/react` 10.62.0 → 10.68.0, `@sentry/vite-plugin` 5.3.0 → 5.4.0, `@tanstack/react-router` 1.170.16 → 1.170.18, `@tanstack/react-virtual` 3.14.4 → 3.14.8, `@tauri-apps/plugin-dialog` 2.7.1 → 2.7.2, Lucide 1.22.0 → 1.26.0, shadcn 4.12.0 → 4.14.1, and `react-resizable-panels` 4.12.0 → 4.12.2.
- Other direct updates: Geist variable font 5.2.9 → 5.3.0, `@pierre/trees` beta.4 → beta.5, and DOMPurify 3.4.11 → 3.4.12.

After the update, `bun outdated` reports no remaining direct updates at either the repository root or `apps/desktop`.

## Applied Rust updates

No direct `Cargo.toml` requirement changes were necessary: the manifests already use current major lines and preserve the required features and platform-specific settings.

`cargo update` refreshed 105 packages in `backend/tauri/Cargo.lock` and 61 packages in `backend/mcp/Cargo.lock`. Notable updates include:

- Tauri 2.11.3 → 2.11.5 and Tauri runtime WRY 2.11.3 → 2.11.4.
- Tokio 1.52.3 → 1.53.1.
- Serde 1.0.228 → 1.0.229 and serde_json 1.0.150 → 1.0.151.
- Rustls 0.23.41 → 0.23.42.
- UUID 1.23.4 → 1.24.0.
- Hyper 1.10.1 → 1.11.0.
- The lockfiles now record the workspace package versions as 0.23.6.

Final `cargo update --dry-run` checks report zero compatible lockfile updates in both workspaces.

## Source migrations

- Added exact root overrides for `@codemirror/state` 6.7.1 and `@codemirror/view` 6.43.6. Bun initially retained older nested CodeMirror copies under `@codemirror/commands`; because CodeMirror types use private identity fields, the mixed copies failed TypeScript checks. The exact overrides deduplicate the family.
- Reviewed the TypeScript 7 migration removals. The project uses `target` ES2021/ES2022 and `moduleResolution: "bundler"` and does not use the compiler options removed in TypeScript 7, so no TypeScript source or tsconfig migration was required.
- No React, Vite, Tailwind, Tauri, or xterm source API changes were required by the accepted versions.

## Security advisories before and after

- Before: 43 advisories — 8 high, 30 moderate, and 5 low.
- After: 43 advisories — 8 high, 30 moderate, and 5 low.

The direct DOMPurify dependency was fixed at 3.4.12, but Mermaid 11.15.0 still resolves DOMPurify 3.4.8 through `streamdown` and `@streamdown/mermaid`. The remaining advisories are in transitive dependencies of shadcn, the Sentry Vite plugin, Streamdown, and Mermaid. No newer compatible direct dependency release in this update removed them.

## Deferred updates and exact blockers

No direct dependency update was deferred.

The 43 transitive JavaScript advisories remain unresolved because the latest direct packages still resolve affected transitive versions. Forcing unrelated transitive overrides without upstream compatibility evidence was not considered safe for this unattended update.

## Validation

- `bun outdated` at the root and in `apps/desktop` before the update: completed; reported the updates applied above.
- `bun audit` before the update: completed with exit 1 because it found 43 advisories (8 high, 30 moderate, 5 low).
- `cargo update --dry-run` in `backend/tauri` and `backend/mcp` before the update: completed; reported 105 and 61 compatible updates respectively.
- `bun update --latest` at the root and in `apps/desktop`: passed. The xterm stable-channel downgrade it proposed was rejected and replaced with the current beta dist-tags.
- `cargo update` in both Rust workspaces: passed.
- `bun install` and the final `bun install --force`: passed. The forced install removed stale nested CodeMirror package directories after the lockfile was deduplicated.
- `bun run typecheck`: passed.
- `(cd apps/desktop && bun run build)`: passed with Vite 8.1.5. Vite emitted its existing large-chunk advisory.
- `(cd backend/tauri && cargo check --workspace)`: passed after staging the repository's two ignored development sidecars.
- `(cd backend/tauri && cargo test --workspace)`: 112 tests passed. The repository's relative macOS Cargo runner path fails for nested workspace packages, so the successful rerun supplied the same checked-in `scripts/dev-sign.sh` runner by absolute path through `CARGO_TARGET_AARCH64_APPLE_DARWIN_RUNNER`.
- `(cd backend/mcp && cargo check)`: passed.
- `(cd backend/mcp && cargo test)`: passed (1 integration test; the binary has no unit tests).
- Final `bun audit`: completed with exit 1 because the same 43 transitive advisories remain.
- Final `cargo update --dry-run` in both Rust workspaces: passed with zero packages to update.
- `cargo audit`: not run because `cargo-audit` is not installed.
