# Dependency updates -- 2026-08-17

## Summary

Updated all direct JavaScript dependencies reported by `bun outdated`, refreshed the maintained xterm beta channel, and refreshed both Rust lockfiles to their latest compatible resolutions. No dependency-required source or configuration migrations were needed, and all prescribed validation passed.

## Applied JavaScript updates

- Root: `turbo` 2.10.9 -> 2.10.10.
- Desktop: `@codemirror/commands` 6.10.4 -> 6.11.0, `@codemirror/view` root override 6.43.8 -> 6.43.9 and direct range `^6.43.7` -> `^6.43.9`, `@sentry/react` 10.69.0 -> 10.70.0, `@tanstack/react-router` 1.170.25 -> 1.170.29, `react-resizable-panels` 4.12.2 -> 4.12.3, `shadcn` 4.16.2 -> 4.18.0, and `zustand` 5.0.14 -> 5.0.15.
- Prerelease checks: `@pierre/trees` beta was already current at 1.0.0-beta.6. The registry `latest` tags for the xterm family point to lower stable versions, so those were not accepted. Same-channel beta tags were applied: `@xterm/addon-fit` 0.12.0-beta.298 -> 0.12.0-beta.299, `@xterm/addon-search` 0.17.0-beta.298 -> 0.17.0-beta.299, `@xterm/addon-webgl` 0.20.0-beta.297 -> 0.20.0-beta.298, and `@xterm/xterm` 6.1.0-beta.300 -> 6.1.0-beta.302.

## Applied Rust updates

- Tauri host direct minimums were advanced to the versions already selected and validated by the lockfile: `tauri-plugin-dialog` 2.7.1 -> 2.7.2, `rusqlite` 0.40.0 -> 0.40.2, `uuid` 1.22.0 -> 1.24.1, `chrono` 0.4.44 -> 0.4.45, and `lru` 0.18.0 -> 0.18.2. The PTY daemon's `uuid` minimum was advanced from 1.22.0 to 1.24.1.
- MCP's direct `rusqlite` minimum was advanced from 0.40.0 to 0.40.2.
- `backend/tauri/Cargo.lock`: 33 compatible updates, including `bstr`, `cc`, `find-msvc-tools`, the `futures` 0.3.34 family, `http-body-util`, ICU 2.3.0 components, `inotify`, `libredox`, `litemap`, `pkg-config`, `potential_utf`, `rustls-webpki`, `uuid`, `zerovec`, and related transitive crates.
- `backend/mcp/Cargo.lock`: 27 compatible updates, including `cc`, `find-msvc-tools`, the applicable `futures` 0.3.34 crates, `http-body-util`, ICU 2.3.0 components, `libredox`, `litemap`, `pkg-config`, `potential_utf`, `rustls-webpki`, `uuid`, `zerovec`, and related transitive crates.
- `cargo-outdated`, `cargo-upgrade`, and `cargo-audit` were not installed. Direct minimums were therefore inspected manually and advanced only where the refreshed lockfile and successful checks supplied a verified compatible version.

## Source migrations

None required.

## Security advisories before and after

`bun audit` reported 64 advisories before and after the update: 15 high, 42 moderate, and 7 low. The remaining advisories are transitive paths through `shadcn`, `streamdown`/Mermaid, Vite, and Sentry tooling; this update did not introduce any new audit findings.

## Deferred updates and exact blockers

No dependency updates were deferred. The 64 remaining JavaScript advisories are transitive dependencies in current upstream graphs and have no verified direct update in this run.

## Validation

- Initial `bun outdated` at the root and in `apps/desktop` -- passed and reported the JavaScript candidates applied above.
- Initial `bun audit` -- completed with exit 1 after reporting 64 advisories (15 high, 42 moderate, 7 low).
- Initial `cargo update --dry-run` in `backend/tauri` -- passed and reported 33 compatible lockfile updates.
- Initial `cargo update --dry-run` in `backend/mcp` -- passed and reported 27 compatible lockfile updates.
- `cargo-outdated`, `cargo-upgrade`, and `cargo-audit` availability checks -- none were installed; no global tools were added.
- `bun update --latest` at the root and in `apps/desktop` -- passed. Explicit registry prerelease checks then kept `@pierre/trees` on beta.6 and advanced the four xterm packages to the current beta tags instead of accepting lower stable releases.
- `bun install` -- passed.
- `bun run typecheck` -- passed (one desktop package).
- `(cd apps/desktop && bun run build)` -- passed. Vite emitted existing future-native-config and chunk-size warnings.
- `bash scripts/build-mcp-sidecar.sh debug && bash scripts/build-pty-daemon-sidecar.sh debug` -- passed and supplied the ignored binaries required by Tauri's checked-in `externalBin` configuration.
- `cargo update` in both Rust workspaces -- passed; the final run after direct-minimum alignment locked 0 additional packages.
- `(cd backend/tauri && cargo check --workspace)` -- passed.
- `(cd backend/tauri && cargo test --workspace)` -- 161 host tests passed and 2 live-server tests were ignored before the repository's relative macOS runner path failed for the nested shared package. Rerunning the workspace tests with the same checked-in `scripts/dev-sign.sh` runner supplied by absolute path passed all packages: 161 host tests, 14 shared tests, and 6 daemon tests; the same 2 live-server tests remained ignored.
- `(cd backend/mcp && cargo check && cargo test)` -- passed; 4 tests passed.
- Final focused rebuilds of the MCP and PTY sidecars and `(cd backend/tauri && cargo check --workspace)` after direct-minimum alignment -- passed.
- `bun audit` -- completed; 64 advisories (15 high, 42 moderate, 7 low), unchanged from before.
- Final `bun outdated` at the root and in `apps/desktop` -- passed with no remaining direct updates reported; prerelease channels were verified separately as described above.
- `git diff --check` -- passed after generated-output cleanup.
